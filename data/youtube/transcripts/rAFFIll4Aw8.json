[{"t": 0.0, "title": "This half-finished string must be broken, right?", "text": "Someone assumes a half-finished JSON string must be broken until the brace arrives. It isn't the SDK hands back a usable object after every chunk. So, how does a partial string already parse as one?"}, {"t": 12.0, "title": "The natural guess: it behaves the same way", "text": "When Claude streams a tool called JSON, parse demands a complete document. Feed it a half-finished string and it throws. So, the natural guess is that a streaming snapshot behaves the same way, broken or empty until the brace lands. Read it early and you'd expect an error, not an object. Here's the concrete case."}, {"t": 28.56, "title": "The anchor: four chunks of one query", "text": "A search tool streams the argument Q solar across four chunks. On input JSON, fires with JSON snapshot equal to then Q, then Q sol, then Q solar. Four snapshots and every single one already a complete parsable object. Here's why the"}, {"t": 43.36, "title": "Why: the stack seals itself shut", "text": "Parser keeps a stack of every object and array it has opened. The instant a chunk ends, it closes each open structure with a zero value default, an empty string, an empty object, and hands back whatever that produces. Nothing waits for the real closing brace. The parser supplies its own. So, back to the search call"}, {"t": 60.48, "title": "The anchor returns: shape safe, value still growing", "text": "Isn't an empty query. It's a query string that hasn't started arriving yet. And Q sol isn't the final query either. Three more characters are still on the wire. A valid snapshot tells you the shape is safe to read. It doesn't tell you the value inside has stopped changing. The parser closes every open"}, {"t": 76.64, "title": "Carry-out", "text": "Structure for you. So, a valid snapshot tells you the shape is safe to read, not that the values inside it are finished. Your turn. Here's the prompt. Read it"}, {"t": 85.16, "title": "Your turn", "text": "With me. I'm streaming a tool call with the Anthropic TypeScript SDK and I see partial JSON arriving via the input JSON event. Walk me through why each chunk's JSON snapshot is already a valid JS object before the closing brace arrives, and show me how to read a field from it safely midstream without waiting for the tool use delta done event. Liam Infobear. Why partial JSON can be a"}, {"t": 106.68, "title": "Outro", "text": "Valid object before the closing brace arrives. Liam Infobear."}]